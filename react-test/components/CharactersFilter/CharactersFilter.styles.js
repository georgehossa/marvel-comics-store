import styled from 'styled-components';

export const Wrapper = styled.section`
  position: relative;
`
export const List = styled.ul`
  display: flex;
  list-style: none;
  margin: 0;
  padding: 1rem 0;
  overflow-x: scroll;
`;
